import React from 'react';

export default React.createClass(
{
    propTypes:
    {
        errors : React.PropTypes.array.isRequired
    },

    render()
    {

        return (
            <div>
                ERRORS
            </div>
        );
    }
} );
